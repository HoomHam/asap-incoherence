# Handoff — ASAP Incoherence Explorer (regenerated by /leave, 2026-08-08 session 5)

## State
- **App LIVE**: https://hoomham.github.io/asap-incoherence/ — CI green, deployed = HEAD (5cd9a59).
- Repos: root `HoomHam/asap-incoherence` (public, Pages CI), workspace
  `HoomHam/asap-incoherence-workspace` (private). Both clean + pushed.
- Sessions 3 (07-20, slice browser + r_N) and 4 (07-23, animation polish) were
  never ledgered — reconciled in the 2026-08-08 ledger entry.
- Session 5 added the **Angles tab**: 3D Thomson spokes + pairwise
  angular-separation histogram. N mirrors the sidebar # charges (NI) live —
  the tab has no N input of its own. Radio switches Thomson optimum vs
  unoptimized Fibonacci; bin 1/2/5°; min/mean + named polyhedra shown.
  Engine: new WASM export `kasap_hedgehog(N, optimize)` (kasap's calchedgehog
  standalone, worker op 'hedgehog', cached, N 2–256, no trajectory regen).

## Key facts (details in workspace/canon/facts.md)
- F18 NEW: the reference slide's angle histograms match UNOPTIMIZED
  Fibonacci-spiral spokes, not Thomson optima (Fib n=26: max-freq 10 @ 1° bins,
  min ~23° — both match slide; true n=12 icosahedron would show only
  63.4°/116.6°/180°, slide shows continuous spread). App's Fibonacci radio
  reproduces the slide's character.
- Validation anchors: N=4 → 109.5° exact (tetrahedron), N=12 → min 63.4°
  (icosahedron), N=26 Thomson min 38.6° / Fib 23.1°, N=32 Thomson 37.4° / Fib 20.7°.

## Open question (Hooman's, explicitly kept open)
- **He still suspects a real app-vs-slide difference beyond F18.** His 6
  comparison screenshots are on Desktop — unreadable to terminal (macOS TCC).
  Next session: have him move them into the repo (or anywhere under ~/Hooman),
  then do a pixel-level overlay of slide vs app histograms per n.

## Next steps (likely order)
1. Resolve the slide-vs-app histogram question (above).
2. Hooman-driven science: rotation-angle slider sweep (~6.5-ilv sub-period, F3),
   n-sweep vs incoherence.
3. Perf if it annoys: full-set PSF ~12 s WASM (no threads on Pages — COOP/COEP).

## Pointers
- Canon: workspace/canon/{ledger,facts,cards,tree}.md
- Angles tab: web/src/main.ts (renderAngles, pairAnglesDeg, hedgehogCache),
  web/src/plots.ts (plotSpokes, plotAngleHist), csrc/kasap_api.c (kasap_hedgehog)
- Slide-match check: Fibonacci formula = kasap calchedgehog init
  (z = 1−2i/(n−1), θ = π(√5−1)i) — quick python in session-5 transcript.
